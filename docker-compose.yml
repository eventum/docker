version: '2.1'

services:
  eventum:
    build: .
    ports:
      - "8088:80"

  mysql:
    image: percona:5.7
    volumes:
      - mysql:/var/lib/mysql
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=1
      - MYSQL_DATABASE=eventum

volumes:
  mysql:

# vim:ts=2:sw=2:et
